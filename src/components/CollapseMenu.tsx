import React from 'react'

function CollapseMenu() {
	// add state for collapsing on click
	// only one category should be open at a time
	// all sub sections of a cagetory can be open at once

	// should take in the props for each question and answer string

	return (
		<div id="menu-container w-full p-4 m-4 bg-[#3d3d3d]">
			CollapseMenu
			<div>Categories</div>
		</div>
	)
}

export default CollapseMenu
